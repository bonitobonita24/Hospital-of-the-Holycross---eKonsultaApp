# eKonsulta Environment Configuration
# Copy to .env for development (not committed to git)
# Production uses config.ini directly with production credentials

# Environment type: development or production
APP_ENV=development

# For development: Set to true to auto-sync config
DEV_MODE=true
